version: '3.7'
services:
  postgres:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: 1q2w3e4R
    volumes:
      - ./data:/var/lib/postgresql/data